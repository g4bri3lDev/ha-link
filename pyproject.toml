[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ha-link"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer",
    "questionary",
    "tomli-w",
]

[project.scripts]
ha-link = "ha_link.cli:app"
